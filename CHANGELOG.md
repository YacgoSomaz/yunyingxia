# 更新日志

## 未发布

- 修复本地开发目录直接启动时显示 `0.0.0-dev` 的问题：源码 `package.json` / `package-lock.json` 同步到 `0.1.24`，无 `commercial-config.json` 时商业配置会从 `package.json` 读取版本。
- 增加配置回归测试，防止开发态版本回退到 `0.0.0` 并误触发更新判断。

## 0.1.24 - 2026-07-17

- 修复覆盖安装时 Inno 默认询问关闭应用、用户点强制关闭仍可能关不掉的问题：安装器不再启用默认关闭页，改为安装前自动强制关闭 `Yunyingxia.exe` 和旧名 `WanshanMedia.exe`，带 `/F /T` 和多轮重试。
- 覆盖安装失败提示改为明确引导用户在任务管理器结束 `Yunyingxia.exe`，避免“按钮没反应”式错误。
- 修复首次手机号登录成功后登录窗口关闭、主窗口尚未创建时 Electron 触发 `window-all-closed` 导致应用退出的问题。
- 确认商业包内 `package.json.version`、`commercial-config.version` 与安装包版本一致，产品固定为 `operation_shrimp`，并避免重复注册 `app:info`。
- 重新打包 `release/operation-shrimp/0.1.24/YunyingxiaSetup_0.1.24.exe`，SHA256 `148a2fbdd630c2a7e15eeb27e33f5d20cc21f5b595819bdc8c73e7fa44ad848a`，大小 `184,665,542` 字节，当前未接入 Authenticode 代码签名。
- 验证结果：`npm test -- --maxWorkers=1 --minWorkers=1` 通过，92 项测试全部通过。

## 0.1.23 - 2026-07-17

- 账号登录流程增加启动保护状态，避免登录窗口和主窗口切换期间被 `window-all-closed` 误判退出。
- 重新打包 `0.1.23` 用于验证登录后不再自动关闭窗口。

## 0.1.22 - 2026-07-17

- 修复 `Attempted to register a second handler for 'app:info'` 启动失败：删除额外 `app:info` IPC 注册，改为用商业配置覆盖 `app.getVersion()` 和 `app.getName()`，让原千山运行时继续提供统一 `app:info`。
- 修复前端显示旧版本导致持续提示更新的问题。

## 0.1.21 - 2026-07-17

- 修复商业构建写入包内版本号，确保前端、主进程和更新器读取同一个版本来源。
- 覆盖安装逻辑新增历史版本登记和安装路径显示，减少用户改过安装目录后快捷方式仍指向旧目录的问题。

## 0.1.20 - 2026-07-17

- 商业包安装器中文化，增加中文 Inno 语言文件。
- 普通/强制更新窗口改为中文，增加下载进度、校验状态和安装状态展示。
- 放宽完整性策略，只校验启动、账号、更新和本地付费拦截关键文件。

## 0.1.13 - 0.1.19 - 2026-07-14 至 2026-07-17

- 完整性校验从“全量文件锁定”调整为“精确文件白名单”：只验证主启动、账号授权、更新验签、本地付费拦截和 `package.json`；用户素材、导出、缓存、数据库、日志、本地配置、前端页面与普通运行时文件均不会因清单外文件阻断启动。
- 白名单内文件仍使用 Ed25519 签名清单校验，缺失或被篡改会阻断启动。
- 新增运行中实时更新通知：运营虾固定监听 `GET https://anyq.site/api/v1/releases/events?product_id=operation_shrimp`，只处理 `release` 事件作为重新检查信号。
- SSE 事件数据不参与版本、下载地址或强制更新判断；每次触发后仍请求固定的 `/api/v1/releases/latest?product_id=operation_shrimp` 并完成 `update-v1` Ed25519 验签、受众、签发/过期时间、版本、安装包域名、大小和 SHA-256 校验。
- 新增 60 秒签名更新检查兜底与 SSE 自动重连；窗口销毁、应用退出时会关闭长连接并清理定时器。
- 运行中校验出 `mandatory=true` 或低于 `min_supported_version` 时禁用主业务窗口；下载、校验或安装失败时明确提示并保留重试，普通更新仅提示不阻断。
- 旧安装包不包含实时监听；重新打包并覆盖安装新版本后才会生效。
- 前端品牌显示名改为“运营虾”，新增白色简约主题覆盖和“虾”字标识。
- 商业启动鉴权从卡密改为手机号账号登录。
- 新增账号服务客户端，接入 `https://anyq.site` 的短信验证码登录、账号状态、充值套餐、微信支付下单和订单状态轮询接口。
- 新手机号登录后按普通用户处理；普通用户默认无会员权益，但可以进入主界面，点击高价值功能时提示开通会员。
- 客户端启动时请求远端 `/api/auth/me`，运行中约 60 秒刷新一次签名 `account_license`；服务端明确停用、过期、禁用或未授权时清除本地权益并阻止付费功能。
- 移除卡密服务和卡密激活窗口源码，构建前清空 `dist-electron`，避免旧卡密 JS 残留进安装包。
- 远端 `recharge-api` 新增 `GET /api/pay/plans` 套餐接口，便于后续价目表由服务端控制。
- 客户端和远端账号接口补充会员字段：`is_member`、`member_level`、`features`、`server_time`、`remaining_days`、`need_recharge`。
- 客户端充值续费改为打开远端网页完成；客户端不再直接创建微信支付订单或展示支付二维码。
- 客户端适配统一 `products[]` 协议，运营虾只检查 `operation_shrimp` 与 `operation_course`，购买复盘虾或漫剧虾不会解锁运营虾。

## 0.1.9 - 2026-07-13

- 客户端商业授权后台自动校验间隔调整为 10 分钟。
- 本地缓存授权已过期时继续清理授权缓存，并通过主进程退出应用，要求重新激活。
- 文案工坊真实模式不再静默降级到 MockLLMClient；缺少 LLM Key 时明确报错。
- 原千山运行时增加本地环境变量 LLM Key 注入，支持 `LLM_API_KEY`、`OPENAI_API_KEY`、`DASHSCOPE_API_KEY`、`DEEPSEEK_API_KEY`、`SILICONFLOW_API_KEY`、`ARK_API_KEY`。
- 设置页增加“万山本地模型配置”，支持本地保存 OpenAI 兼容模型 Base URL、模型名和 API Key；本地配置优先于千山云端 LLM 配置。
- 本地 LLM API Key 使用 Electron `safeStorage` 加密保存，界面和接口只返回脱敏状态。
- 本地模型配置面向中转站适配：默认使用“自定义中转站”，允许填写客户自己的 OpenAI 兼容 Base URL、模型/接入点 ID 和 API Key，不做厂商白名单限制。
- 图片搜索真实模式不再在无图源 Key 或零结果时返回 mock 占位图。
- 设置页增加“数字人供应商配置”，支持阿里万相 `wan2.2-s2v` 和百度曦灵照片数字人。
- 阿里万相数字人优先读取本地百炼 Key，百度曦灵读取本地 AppID/AppKey；数字人配置接口支持保存和测试。

## 0.1.8 - 2026-07-13

- 修复安装后启动失败：第三方依赖的 `package.json` 入口指向 `src/...`，发布清理删除 `src/` 后导致 Electron 无法加载模块。
- 构建脚本新增运行时依赖入口迁移：发布前将依赖包内必要 `src` 入口复制到 `dist`，并把 `main/module` 改写到 `./dist/...`，再执行源码清理。
- 保持商业包内不出现 `src/` 目录，同时确保依赖入口文件完整。
- 新增构建测试覆盖该迁移规则。
- 重新打包商业安装包 `WanshanMediaSetup_0.1.8.exe`。
- 通过 asar 扫描确认无禁用源码文件、无缺失依赖入口、完整性清单已签名。
- 通过 Playwright/Electron 启动冒烟测试，主界面可正常进入。

## 0.1.7 - 2026-07-12

- 新增万山自媒体独立授权产品码 `wanshan_zimeiti`。
- 远端管理后台增加“万山自媒体”，与“万山漫剧”和“直播复盘侠”隔离。
- 万山自媒体卡密前缀改为 `WSZ-`，授权包必须包含 `product_code=wanshan_zimeiti`。
- 客户端商业配置默认产品码改为 `wanshan_zimeiti`。
- 验证万山自媒体卡无法激活万山漫剧，错误返回为产品不匹配。
- 重新打包商业安装包 `WanshanMediaSetup_0.1.7.exe`。
- 更新线上 `latest.json/latest.yml` 到 `0.1.7`。

## 0.1.6 - 2026-07-11

- 修复授权缓存导致卡密过期或冻结后仍可继续使用的问题。
- 客户端启动时主动刷新授权。
- 商业版客户端运行中约 60 秒刷新一次授权。
- 服务端明确拒绝授权时，客户端清除本地授权缓存。
- 显式过期卡密不再额外加离线宽限。
- 加固发布脚本，清理 `src/`、测试目录、源码映射、数据库、密钥、C/C++ 源码等残留。
- 重新打包商业安装包 `WanshanMediaSetup_0.1.6.exe`。

## 0.1.5 - 2026-07-11

- 发布商业安装包 `WanshanMediaSetup_0.1.5.exe`。
- 生成签名完整性清单。
- 上传安装包和更新清单到 `https://license.runmo.art/wanshan-media/updates/`。
- 验证安装包可通过 Range 下载。
- 初步接入远端授权后台和卡密创建流程。

## 0.1.4 - 2026-07-11

- 增加国内更新器发布配置。
- 生成 `latest.json/latest.yml`。
- 加强安装目录完整性校验。
- 禁用开发者工具和危险调试入口。

## 0.1.3 及更早

- 迁移原千山 UI/运行时。
- 修复提示词模板、风格预设和下拉菜单空缺。
- 恢复一键生成、视频生成、AI 视频服务等原千山后端流程。
- 添加 Playwright 自动化检查脚本。
- 添加 Git LFS 管理大安装包和媒体二进制。
